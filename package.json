{
  "name": "finora",
  "description": "Crypto trading app",
  "author": "Jozsef Vass",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --stream -r run dev",
    "build": "pnpm --parallel --stream -r run build",
    "lint": "pnpm --parallel --stream -r run lint",
    "typecheck": "pnpm --parallel --stream -r run typecheck",
    "test": "pnpm --parallel --stream -r run test",
    "db:generate": "pnpm --parallel --stream -r run db:generate",
    "db:migrate": "pnpm --parallel --stream -r run db:migrate",
    "coverage": "pnpm --parallel --stream -r run coverage",
    "syncpack:check": "syncpack list-mismatches",
    "syncpack:fix": "syncpack fix-mismatches"
  },
  "packageManager": "pnpm@10.12.4",
  "devDependencies": {
    "prettier": "^3.6.2",
    "syncpack": "^13.0.4"
  }
}
